/**
 * Funcion que maneja la ip del servidor al que apuntan las apis cuando se genera el build prod**/
export const environment = {
  production: true,
  http_server: 'https://pruebatecnica.puntosleal.com'
};
